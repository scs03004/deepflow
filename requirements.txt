# Core dependencies (minimal for better compatibility)
networkx>=2.8             # Graph analysis (older version for compatibility)
matplotlib>=3.5.0         # Graph visualization
plotly>=5.0.0             # Interactive plots
rich>=12.0.0              # Rich terminal output (older version)

# Data formats
pandas>=1.4.0             # Data analysis (older version)

# Git integration
GitPython>=3.1.0          # Git operations

# Web dashboard (optional)
flask>=2.0.0              # Web framework (older version)

# Development dependencies
pytest>=7.0.0             # Testing framework
pytest-cov>=4.0.0         # Coverage reporting
black>=22.0.0             # Code formatting (older version)
mypy>=0.950               # Type checking (older version)
flake8>=4.0.0             # Linting (older version)